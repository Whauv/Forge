# Tests

This folder mirrors the backend package layout for Python tests.

- `core/` contains tests for shared backend helpers.
- Additional folders can be added for `agents/`, `api/`, `db/`, and `graph/` as coverage expands.
