# API Routes

This folder contains the HTTP route modules for the backend.

- `ingest.py` handles project ingestion requests.
- `analyze.py` handles analysis and streamed task-planning responses.
- `run.py` executes task coding and testing pipelines.
- `deploy.py` applies approved diffs and records deployments.
