# Core Helpers

This folder contains shared backend helpers that do not belong to a single route or agent.

- `config.py` loads required environment variables.
- `json_stream.py` provides helpers for safe JSON event streaming.
- `repo_utils.py` contains GitHub repository parsing and artifact normalization utilities.
